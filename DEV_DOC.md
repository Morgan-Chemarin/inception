diffrecne VM / docker container 

VM = ordinateur entier: systeme dexploitation, noyau, ram
docker = cest une petite application isolé. utilise le systeme dexploitation de sa machine + le minimum pour faire tourner lapplication


difference image container
image = le plan pour faire le cont
ainer (installe le package, ouvre le port)
container = cest linstance de limage, on peut faire plusieurs container a partir d une image


docker-compose

c'est le fichier de config qui va demarer les containers et les reseaux dans le bon ordre quand on fait (docker compose up / ou make plus tard je crois)


docker named volume


docker network


